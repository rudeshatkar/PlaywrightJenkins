
pipeline {
  agent any

  tools {nodejs "NODEJS"}
 
  stages {
    stage('install playwright') {
        steps {
        sh '''
          npx playwright install
          npm i cucumber
        '''
      }
    }
    stage('help') {
      steps {
        sh 'npx playwright test --help'
      }
    }
    stage('Run tests'){
      steps {
        
          sh 'npx playwright test'
        
        
      }
    }
  }

}